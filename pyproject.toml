[build-system]
requires = ["uv_build >= 0.9.22, < 0.10.0"]
build-backend = "uv_build"

[project]
name = "pybvrf"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "jsonschema>=4.26.0",
    "mne>=1.11.0",
    "numpy>=2.2.0",
]
